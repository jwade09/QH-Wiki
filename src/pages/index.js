import * as React from "react"
import { useStaticQuery, graphql } from "gatsby"

// markup
const IndexPage = () => {
  return (
    <main>
      <h1>Hello world!</h1>
    </main>
  )
}

export default IndexPage
